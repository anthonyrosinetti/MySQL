CASE
	WHEN SOURCE_NAME NOT IN ('Google Ads', 'Meta Ads') THEN SOURCE_NAME
    WHEN REGEXP_CONTAINS(CAMPAIGN_GROUP,'Google') THEN 'Google Ads'
    WHEN REGEXP_CONTAINS(CAMPAIGN_GROUP,'Youtube') THEN 'Youtube Ads'
    ELSE SOURCE_NAME
END
