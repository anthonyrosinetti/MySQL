(SUM(CASE WHEN DATETIME_DIFF(DATE_ACTUAL,DATE_COHORT,MONTH) <= 1 AND DATETIME_DIFF(CURRENT_DATE(),DATE_COHORT,MONTH) >= 1 THEN PURCHASE_AMOUNT_BY_FIRST_PURCHASE_DATE
END)-SUM(CASE WHEN DATETIME_DIFF(DATE_ACTUAL,DATE_COHORT,MONTH) <= 1 AND DATETIME_DIFF(CURRENT_DATE(),DATE_COHORT,MONTH) >= 1 THEN COST_BY_FIRST_PURCHASE_DATE
END))/SUM(CASE WHEN DATETIME_DIFF(DATE_ACTUAL,DATE_COHORT,MONTH) = 0 THEN FIRST_SUBSCRIPTIONS_BY_FIRST_SUBSCRIPTION_DATE END)
