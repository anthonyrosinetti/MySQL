(SUM(CASE
      WHEN DATETIME_DIFF(DATE_ACTUAL, DATE_COHORT, MONTH) >= 0 THEN PURCHASE_AMOUNT_BY_FIRST_PURCHASE_DATE END) - SUM(CASE
      WHEN DATETIME_DIFF(DATE_ACTUAL, DATE_COHORT, MONTH) >= 0 THEN COST_BY_FIRST_PURCHASE_DATE END)) / SUM(CASE
    WHEN DATETIME_DIFF(DATE_ACTUAL, DATE_COHORT, MONTH) = 0 THEN FIRST_PURCHASES_BY_FIRST_PURCHASE_DATE END)
