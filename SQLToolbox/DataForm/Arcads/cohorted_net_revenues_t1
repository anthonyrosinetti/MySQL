SUM(CASE WHEN DATETIME_DIFF(DATE_ACTUAL,DATE_COHORT,MONTH) <= 1 AND DATETIME_DIFF(CURRENT_DATE(),DATE_COHORT,MONTH) >= 1 THEN PURCHASE_AMOUNT_BY_SIGNUP_DATE
END)
-
SUM(CASE WHEN DATETIME_DIFF(DATE_ACTUAL,DATE_COHORT,MONTH) <= 1 AND DATETIME_DIFF(CURRENT_DATE(),DATE_COHORT,MONTH) >= 1 THEN COST_BY_SIGNUP_DATE
END)
